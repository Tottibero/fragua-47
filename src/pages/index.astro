---
import Layout from '../layouts/Layout.astro';
---

<Layout description="Fragua47, comunidad BDSM. Espacio seguro, consensuado y de confianza.">
	<link slot="head" rel="preload" as="image" type="image/webp" href="/juguetes-f47.webp" />
	<article>
		<img src="/juguetes-f47.webp" alt="Juguetes F47" class="portada" loading="eager" fetchpriority="high" width="799" height="800" />
		<div class="text-col">
			<p class="pre-titulo">— bienvenida —</p>
			<h2>Fragua47</h2>
			<hr class="linea-titulo" />
			<p>Un <strong>local de BDSMK</strong> en <strong>Valladolid</strong> donde se organizan <strong>fiestas, charlas y talleres</strong> para los amantes de este mundillo.</p>
			<p>Un espacio asociado en el que poder disfrutar y aprender del BDSMK.</p>
			<p>El proyecto nace de una locura colectiva de unos pocos para revivir el panorama bedesemero en Valladolid y tener un sitio propio para seguir compartiendo y aprendiendo de manera <strong>segura y libre</strong>.</p>
			<p>Siéntete libre de compartir con nosotros la experiencia y de sumarte a la iniciativa y aportar. No sería posible sin ti.</p>
			<div class="btns-bienvenida">
				<a href="https://t.me/+dFpFhSU40jhhMDVk" class="btn-social" target="_blank" rel="noopener noreferrer">
					<svg aria-hidden="true" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M11.944 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0a12 12 0 0 0-.056 0zm4.962 7.224c.1-.002.321.023.465.14a.506.506 0 0 1 .171.325c.016.093.036.306.02.472-.18 1.898-.962 6.502-1.36 8.627-.168.9-.499 1.201-.82 1.23-.696.065-1.225-.46-1.9-.902-1.056-.693-1.653-1.124-2.678-1.8-1.185-.78-.417-1.21.258-1.91.177-.184 3.247-2.977 3.307-3.23.007-.032.014-.15-.056-.212s-.174-.041-.249-.024c-.106.024-1.793 1.14-5.061 3.345-.48.33-.913.49-1.302.48-.428-.008-1.252-.241-1.865-.44-.752-.245-1.349-.374-1.297-.789.027-.216.325-.437.893-.663 3.498-1.524 5.83-2.529 6.998-3.014 3.332-1.386 4.025-1.627 4.476-1.635z"/></svg>
					Telegram
				</a>
				<a href="https://fetlife.com/Fragua47" class="btn-social" target="_blank" rel="noopener noreferrer">
					<span class="fet-icon" aria-hidden="true">f</span>
					FetLife
				</a>
			</div>
		</div>
	</article>

	<section class="evento">
		<div class="text-col-evento">
			<div class="evento-header">
				<img src="/VectLogoF47.webp" alt="Logo Fragua47" class="evento-logo" width="300" height="343" />
				<p class="pre-titulo">— próxima fiesta —</p>
			</div>
			<h3>Fetish Uniform Night</h3>
			<hr class="linea" />
			<p class="cuerpo">El 1 de mayo es el Día del Trabajador. Nosotros lo celebramos a nuestra manera: con uniforme. Ven vestido para la ocasión.</p>
			<div class="datos-evento">
				<div class="dato-fila">
					<span class="dato-label">
						<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
						Fecha
					</span>
					<span class="dato-valor">2 de Mayo</span>
				</div>
				<div class="dato-fila">
					<span class="dato-label">
						<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
						Hora
					</span>
					<span class="dato-valor">21:00 h</span>
				</div>
				<div class="dato-fila">
					<span class="dato-label">
						<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>
						Dress code
					</span>
					<span class="dato-valor">Uniforme, fetish ó mínimo negro</span>
				</div>
				<div class="dato-fila">
					<span class="dato-label">
						<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
						Lugar
					</span>
					<span class="dato-valor dato-privado">Se indicará al confirmar asistencia</span>
				</div>
			</div>
			<hr class="linea" />
			<div class="footer-card">
				<button class="btn-inscribete" id="btn-inscribete">Inscríbete ⚒</button>
			</div>
		</div>

		<img src="/fetish-uniform.png" alt="Fetish Uniform Night" class="img-evento" loading="lazy" width="797" height="453" />
	</section>

	<!-- Modal -->
	<div class="modal-overlay" id="modal-overlay">
		<div class="modal-documento">
			<button class="modal-cerrar" id="modal-cerrar" aria-label="Cerrar">✕</button>
			<div class="doc-header">
				<img src="/VectLogoF47.webp" alt="Logo Fragua47" class="modal-logo" width="300" height="343" />
				<h4>Inscripción</h4>
				<span class="doc-sello">⚒</span>
			</div>
			<p class="doc-sub">— Fetish Uniform Night · Fragua47 · Valladolid —</p>
			<hr class="doc-linea" />
			<div class="form-exito" id="inscripcion-exito">
				<svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
				<p class="exito-titulo">¡Inscripción enviada!</p>
				<p class="exito-sub">Te confirmamos pronto.</p>
			</div>

			<form id="form-inscripcion">
				<div class="fila-campos">
					<div class="campo">
						<label for="correo-evento">Correo <span class="req">*</span></label>
						<input type="email" id="correo-evento" name="correo" required placeholder="tu@correo.com" />
					</div>
					<div class="campo">
						<label for="alias-evento">Alias <span class="req">*</span></label>
						<input type="text" id="alias-evento" name="alias" required placeholder="¿Cómo te conocemos?" />
					</div>
				</div>
				<div class="campo">
					<label for="fetlife-evento">FetLife <span class="opcional">(opcional)</span></label>
					<input type="text" id="fetlife-evento" name="fetlife" placeholder="Tu nick en FetLife" />
				</div>
				<hr class="doc-linea" />
				<p class="privacidad-nota">Al enviar aceptas nuestra <a href="/privacidad/">política de privacidad</a>.</p>
				<div class="doc-footer">
					<p class="form-msg" id="inscripcion-msg"></p>
					<button type="submit" id="inscripcion-btn" class="btn-enviar">Enviar inscripción ⚒</button>
				</div>
			</form>
		</div>
	</div>
</Layout>

<script is:inline>
	const btn     = document.getElementById('btn-inscribete');
	const overlay = document.getElementById('modal-overlay');
	const cerrar  = document.getElementById('modal-cerrar');

	btn?.addEventListener('click', () => overlay.classList.add('abierto'));
	cerrar?.addEventListener('click', () => overlay.classList.remove('abierto'));
	overlay?.addEventListener('click', (e) => {
		if (e.target === overlay) overlay.classList.remove('abierto');
	});
	document.addEventListener('keydown', (e) => {
		if (e.key === 'Escape') overlay?.classList.remove('abierto');
	});

	document.getElementById('form-inscripcion')?.addEventListener('submit', async (e) => {
		e.preventDefault();
		const form = e.target;
		const ibtn = document.getElementById('inscripcion-btn');
		const msg  = document.getElementById('inscripcion-msg');
		ibtn.disabled = true;
		ibtn.textContent = 'Enviando...';
		msg.className = 'form-msg';
		msg.textContent = '';
		const correo  = document.getElementById('correo-evento').value.trim();
		const alias   = document.getElementById('alias-evento').value.trim();
		const fetlife = document.getElementById('fetlife-evento').value.trim();
		const res = await fetch('/api/inscripcion', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ correo, alias, fetlife }) });
		if (res.ok) {
			form.style.display = 'none';
			const exito = document.getElementById('inscripcion-exito');
			exito.style.display = 'flex';
		} else {
			msg.className = 'form-msg error';
			msg.textContent = 'Error al enviar. Inténtalo de nuevo.';
		}
		ibtn.disabled = false;
		ibtn.textContent = 'Enviar inscripción ⚒';
	});
</script>

<style>
	article {
		width: 100%;
		margin: 0.8rem auto 1.8rem;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1.4rem;
		align-items: stretch;
		font-family: 'Cinzel', serif;
		color: #3d2010;
	}

	h2 {
		font-family: 'Cinzel', serif;
		font-size: 1.1rem;
		letter-spacing: 4px;
		text-transform: uppercase;
		color: #2a1208;
		margin: 0 0 0.15rem;
		text-align: center;
	}

	.pre-titulo {
		font-size: 0.5rem;
		letter-spacing: 2px;
		text-transform: uppercase;
		color: #5a3018;
		margin-bottom: 0.15rem;
		text-align: center;
	}

	.linea-titulo {
		border: none;
		border-top: 1.5px solid #2a1208;
		margin: 0.25rem 0 0.45rem;
	}

	p {
		font-family: system-ui, -apple-system, sans-serif;
		font-size: 0.72rem;
		line-height: 1.55;
		margin-bottom: 0.3rem;
		color: #2a1208;
	}

	.portada {
		width: 100%;
		height: 100%;
		max-height: 300px;
		object-fit: cover;
		display: block;
		border: 3px solid #2a1208;
		outline: 1px solid #2a1208;
		outline-offset: 4px;
		box-shadow: 5px 5px 0 #2a1208;
	}

	.text-col {
		background: #d4b896;
		border: 3px solid #2a1208;
		outline: 1px solid #2a1208;
		outline-offset: 4px;
		box-shadow: 5px 5px 0 #2a1208;
		padding: 0.75rem 1rem;
		display: flex;
		flex-direction: column;
		justify-content: center;
	}

	.btns-bienvenida {
		display: flex;
		gap: 0.6rem;
		flex-wrap: wrap;
		margin-top: 1rem;
	}

	.btn-social {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		padding: 0.45rem 1rem;
		background: #b44814;
		color: #fff7ee;
		border: none;
		box-shadow: 3px 3px 0 #6a2808;
		font-family: 'Cinzel', serif;
		font-size: 0.5rem;
		letter-spacing: 2px;
		text-transform: uppercase;
		text-decoration: none;
		cursor: pointer;
		transition: background 0.2s, box-shadow 0.2s;
	}

	.btn-social:hover {
		background: #d45a18;
		box-shadow: 4px 4px 0 #6a2808;
	}

	.btn-social svg {
		width: 14px;
		height: 14px;
		flex-shrink: 0;
	}

	.fet-icon {
		font-family: Georgia, serif;
		font-style: italic;
		font-size: 1rem;
		line-height: 1;
		font-weight: 700;
	}

	/* ── Tarjeta evento ── */

	.evento {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1.4rem;
		align-items: stretch;
		margin: 0 auto 1rem;
	}

	.img-evento {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: 64% center;
		display: block;
		border: 3px solid #2a1208;
		outline: 1px solid #2a1208;
		outline-offset: 4px;
		box-shadow: 5px 5px 0 #2a1208;
	}

	.text-col-evento {
		background: #d4b896;
		border: 3px solid #2a1208;
		outline: 1px solid #2a1208;
		outline-offset: 4px;
		box-shadow: 5px 5px 0 #2a1208;
		padding: 0.75rem 1rem;
		display: flex;
		flex-direction: column;
		font-family: 'Cinzel', serif;
	}

	h3 {
		font-family: 'Metamorphous', cursive;
		font-size: 1.3rem;
		letter-spacing: 2px;
		text-transform: uppercase;
		color: #2a1208;
		text-align: center;
		margin-bottom: 0.15rem;
		line-height: 1.15;
	}

	.linea {
		border: none;
		border-top: 1.5px solid #2a1208;
		margin: 0.35rem 0;
	}

	.evento-header {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.15rem;
		margin-bottom: 0.15rem;
	}

	.evento-logo {
		height: 32px;
		width: auto;
		opacity: 0.9;
	}

	.sentidos {
		list-style: none;
		padding: 0;
		margin: 0 0 0.45rem;
		display: grid;
		grid-template-columns: repeat(5, 1fr);
		gap: 0.3rem;
	}

	.sentidos li {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 0.15rem;
		padding: 0.3rem 0.15rem;
		font-family: 'Cinzel', serif;
		font-size: 0.45rem;
		letter-spacing: 1.5px;
		text-transform: uppercase;
		color: #fff;
		font-weight: 700;
	}

	.sentidos li svg {
		width: 0.8rem;
		height: 0.8rem;
		stroke: currentColor;
	}

	.s-vista  { background: #c03818; box-shadow: 2px 2px 0 rgba(0,0,0,0.3); }
	.s-oido   { background: #7a5200; box-shadow: 2px 2px 0 rgba(0,0,0,0.3); }
	.s-olfato { background: #7c3890; box-shadow: 2px 2px 0 rgba(0,0,0,0.3); }
	.s-gusto  { background: #b82850; box-shadow: 2px 2px 0 rgba(0,0,0,0.3); }
	.s-tacto  { background: #206858; box-shadow: 2px 2px 0 rgba(0,0,0,0.3); }

	.cuerpo {
		font-family: system-ui, -apple-system, sans-serif;
		font-size: 0.7rem;
		color: #2a1208;
		line-height: 1.55;
		margin-bottom: 0 !important;
		flex: 1;
		text-align: center;
	}

	.datos-evento {
		margin: 0.4rem 0 0.3rem;
		display: flex;
		flex-direction: column;
		gap: 0;
		border-top: 1px solid rgba(42,18,8,0.25);
		border-bottom: 1px solid rgba(42,18,8,0.25);
		padding: 0.3rem 0;
	}

	.dato-fila {
		display: flex;
		align-items: baseline;
		gap: 0.6rem;
		padding: 0.18rem 0;
	}

	.dato-fila + .dato-fila {
		border-top: 1px solid rgba(42,18,8,0.15);
	}

	.dato-label {
		font-family: 'Cinzel', serif;
		font-size: 0.58rem;
		letter-spacing: 2px;
		text-transform: uppercase;
		color: #6a2808;
		font-weight: 700;
		min-width: 3.8rem;
		flex-shrink: 0;
		display: flex;
		align-items: center;
		gap: 0.3rem;
	}

	.dato-label svg {
		width: 0.75rem;
		height: 0.75rem;
		flex-shrink: 0;
	}

	.dato-valor {
		font-family: system-ui, -apple-system, sans-serif;
		color: #2a1208;
		font-size: 0.78rem;
		font-weight: 600;
	}

	.dato-privado {
		font-size: 0.78rem;
		font-weight: 700;
		color: #2a1208;
	}

	.footer-card {
		display: flex;
		justify-content: center;
	}

	.btn-inscribete {
		background: #b44814;
		color: #fff7ee;
		border: none;
		padding: 0.7rem 1.6rem;
		font-family: 'Cinzel', serif;
		font-size: 0.75rem;
		letter-spacing: 3px;
		text-transform: uppercase;
		cursor: pointer;
		transition: background 0.2s, box-shadow 0.2s;
		box-shadow: 3px 3px 0 #6a2808;
	}

	.btn-inscribete:hover {
		background: #d45a18;
		box-shadow: 4px 4px 0 #6a2808;
	}

	/* ── Modal ── */

	.modal-overlay {
		display: none;
		position: fixed;
		inset: 0;
		background: rgba(20, 8, 2, 0.75);
		z-index: 500;
		align-items: center;
		justify-content: center;
	}

	.modal-overlay.abierto {
		display: flex;
	}

	.modal-documento {
		background: #d4b896;
		border: 3px solid #2a1208;
		outline: 1px solid #2a1208;
		outline-offset: 5px;
		box-shadow: 8px 8px 0 #2a1208;
		padding: 1.8rem 2rem;
		width: 100%;
		max-width: 480px;
		position: relative;
		font-family: 'Cinzel', serif;
	}

	.modal-cerrar {
		position: absolute;
		top: 0.6rem;
		right: 0.8rem;
		background: none;
		border: none;
		font-size: 1rem;
		color: #2a1208;
		cursor: pointer;
		line-height: 1;
		padding: 0;
	}

	.modal-cerrar:hover { color: #b44814; }

	.doc-header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.8rem;
		margin-bottom: 0.3rem;
	}

	.modal-logo {
		height: 24px;
		width: auto;
		flex-shrink: 0;
	}

	.doc-sello {
		font-size: 1.2rem;
		font-weight: 900;
		color: #b44814;
		letter-spacing: 2px;
	}

	h4 {
		font-size: 0.95rem;
		letter-spacing: 4px;
		text-transform: uppercase;
		color: #2a1208;
		margin: 0;
		text-align: center;
		font-family: 'Cinzel', serif;
	}

	.doc-sub {
		font-size: 0.55rem;
		letter-spacing: 1.5px;
		color: #5a3018;
		text-align: center;
		margin: 0 0 0.4rem;
		text-transform: uppercase;
	}

	.doc-linea {
		border: none;
		border-top: 1.5px solid #2a1208;
		margin: 0.8rem 0;
	}

	.fila-campos {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1rem;
	}

	.campo {
		display: flex;
		flex-direction: column;
		margin-bottom: 0.9rem;
	}

	label {
		font-family: 'Cinzel', serif;
		font-size: 0.6rem;
		letter-spacing: 2px;
		text-transform: uppercase;
		color: #2a1208;
		margin-bottom: 0.3rem;
		font-weight: 700;
	}

	.req { color: #b44814; }

	.opcional {
		font-family: system-ui, -apple-system, sans-serif;
		font-weight: 400;
		text-transform: none;
		letter-spacing: 0;
		color: #5a3018;
		font-size: 0.65rem;
	}

	input {
		background: rgba(255,255,255,0.3);
		border: none;
		border-bottom: 1.5px solid #2a1208;
		padding: 0.4rem 0.3rem;
		font-family: system-ui, -apple-system, sans-serif;
		font-size: 0.82rem;
		color: #1c0a02;
		outline: none;
		transition: border-color 0.2s, background 0.2s;
	}

	input:focus {
		border-color: #b44814;
		background: rgba(255,255,255,0.5);
	}

	input::placeholder {
		color: #7a5035;
		font-style: italic;
		font-size: 0.75rem;
	}

	.doc-footer {
		display: flex;
		align-items: center;
		justify-content: flex-end;
		gap: 0.8rem;
		flex-wrap: wrap;
	}

	.form-msg {
		font-family: system-ui, -apple-system, sans-serif;
		font-size: 0.72rem;
		margin: 0;
	}
	.form-msg.ok    { color: #2a6a1a; }
	.form-msg.error { color: #b41414; }
	.privacidad-nota { font-family: system-ui, -apple-system, sans-serif; font-size: 0.62rem; color: #7a5035; margin: 0.2rem 0 0.5rem; text-align: right; }
	.privacidad-nota a { color: #6a2808; }

	.form-exito {
		display: none;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 0.8rem;
		padding: 2rem 1rem;
		text-align: center;
	}
	.form-exito svg {
		width: 72px;
		height: 72px;
		stroke: #2a6a1a;
	}
	.exito-titulo {
		font-family: 'Cinzel', serif;
		font-size: 1rem;
		letter-spacing: 3px;
		text-transform: uppercase;
		color: #2a1208;
		margin: 0;
		font-weight: 700;
	}
	.exito-sub {
		font-family: system-ui, -apple-system, sans-serif;
		font-size: 0.82rem;
		color: #5a3018;
		margin: 0;
	}

	.btn-enviar {
		background: #b44814;
		color: #fff7ee;
		border: none;
		padding: 0.7rem 1.4rem;
		font-family: 'Cinzel', serif;
		font-size: 0.72rem;
		letter-spacing: 2px;
		text-transform: uppercase;
		cursor: pointer;
		transition: background 0.2s, box-shadow 0.2s;
		box-shadow: 3px 3px 0 #6a2808;
	}

	.btn-enviar:hover {
		background: #d45a18;
		box-shadow: 4px 4px 0 #6a2808;
	}

	@media (max-width: 768px) {
		article, .evento {
			grid-template-columns: 1fr;
			margin: 1.5rem auto;
			gap: 1.5rem;
		}
		.fila-campos { grid-template-columns: 1fr; }
		.modal-documento { margin: 1rem; }
	}

	::selection {
		background: rgba(180, 80, 20, 0.35);
		color: #ffe0b0;
	}
</style>
